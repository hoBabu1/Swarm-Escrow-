{
  "name": "swarm-escrow-oracle",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Polling oracle worker for Swarm Escrow: fetches submitted deliverables, runs the 4 AI agent roles, and posts verdicts on-chain.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "@supabase/supabase-js": "^2.45.4",
    "dotenv": "^16.4.5",
    "ethers": "^6.13.4",
    "octokit": "^4.0.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=20"
  }
}
